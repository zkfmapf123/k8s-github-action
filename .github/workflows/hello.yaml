name: Hello World

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: [runner]
    steps:
      - name: Hello World
        run: echo "Hello, World!"
