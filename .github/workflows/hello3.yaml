name: Hello World 3

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: self-hosted
    steps:
      - name: Hello World 3
        run: echo "Hello, World! 3"
      - name: CPU Load Test
        run: |
          for i in {1..1000000}; do
            echo "scale=1000; 4*a(1)" | bc -l
          done 